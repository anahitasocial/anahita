<?php 

dbexec("
CREATE TABLE IF NOT EXISTS `#__opensocial_profiles` (
  `id` bigint(11) unsigned NOT NULL AUTO_INCREMENT,
  `socialengine_actor_id` bigint(11) NOT NULL,
  `honorific_prefix` varchar(250) NULL,
  `honorific_suffix` varchar(250) NULL,
  `build` varchar(250) NULL,
  `eye_color` varchar(250) NULL,
  `hair_color` varchar(250) NULL,
  `height` varchar(250) NULL,
  `weight` varchar(250) NULL,
  `activities` text NULL,
  `books` text NULL,
  `cars` text NULL,
  `children` varchar(255) NULL,
  `date_of_birth` varchar(10) DEFAULT NULL,
  `drinker` varchar(255) NULL,
  `ethnicity` varchar(100) NULL,
  `fashion` varchar(100) NULL,
  `food` text NULL,
  `happiest_when` varchar(255) NULL,
  `heroes` text NULL,
  `humor` text NULL,
  `interests` text NULL,
  `job_interests` varchar(255) NULL,
  `living_arrangement` varchar(255) NULL,
  `looking_for` varchar(255) NULL,
  `movies` text NULL,
  `music` text NULL,
  `nickname` varchar(255) NULL,
  `pets` varchar(100) NULL,
  `political_views` varchar(255) NULL,
  `profile_song` varchar(255) NULL,
  `profile_url` varchar(255) NULL,
  `profile_video` varchar(255) NULL,
  `quotes` text NULL,
  `relationship_status` varchar(255) NULL,
  `religion` varchar(255) NULL,
  `romance` varchar(255) NULL,
  `scared_of` varchar(255) NULL,
  `sexual_orientation` varchar(255) NULL,
  `smoker` varchar(255) NULL,
  `sports` text NULL,
  `turn_offs` text NULL,
  `turn_ons` text NULL,
  `tv_shows` text NULL,
  `urls` text NULL,
  PRIMARY KEY (`id`),
  KEY `build` (`build`),
  KEY `eye_color` (`eye_color`),
  KEY `hair_color` (`hair_color`),
  KEY `ethnicity` (`ethnicity`),
  KEY `religion` (`religion`),
  KEY `drinker` (`drinker`),
  KEY `smoker` (`smoker`),
  KEY `political_views` (`political_views`),
  KEY `sexual_orientation` (`sexual_orientation`),
  KEY `relationship_status` (`relationship_status`),
  KEY `looking_for` (`looking_for`),
  KEY `socialengine_actor_id` (`socialengine_actor_id`)
) ENGINE=InnoDB  DEFAULT CHARSET=utf8");

?>